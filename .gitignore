node_modules/
*.log
.env
dist/
.DS_Store

# ── Phase-1 baseline additions ─────────────────────────────────────

# Real env files (every variant — NEVER commit)
.env.local
.env.production
.env.production-staging
.env.development
.env.development.local
.env.test
.env.test.local
.env.staging
.env.*.local
.env.*.production

# Backend env paths (defensive — backend has its own .env files)
backend/.env
backend/.env.local
backend/.env.production
backend/.env.production-staging
backend/.env.development
backend/.env.test
backend/.env.staging
backend/.env.*.local
backend/.env.*.production

# Env backups (CRITICAL: backend/.env.bak.20260505 already exists with secrets)
.env.bak
.env.bak.*
.env.backup
backend/.env.bak
backend/.env.bak.*
backend/.env.backup
*.bak
backups/

# Build outputs
backend/dist/
frontend/dist/
frontend/build/
frontend/.vite/
out/
build/

# Caches
.turbo/
.cache/
.eslintcache
*.tsbuildinfo
coverage/
.nyc_output/

# Logs
backend/logs/
logs/
pm2-*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / IDE
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*.swo

# DB local files (defensive — we use SQL Server remote)
*.mdf
*.ldf

# Generated migration helpers (PENDING.md is the only md we keep)
database/migrations/*.tmp

# Runtime user-generated content (uploads grow over time, not source)
backend/uploads/
uploads/

# TLS / certificates / private keys (never commit)
*.pem
*.key
*.crt
*.cer
*.p12
*.pfx
*.jks
*.keystore
backend/certs/
certs/
